---
# Ansible Galaxy collection dependencies for NexusLiberty WAS automation.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5.0"
